# Entrada de dados
tempo_viagem = int(input())
velocidade_media = int(input())

# Cálculo da distância
distancia = tempo_viagem * velocidade_media

# Cálculo da quantidade de litros necessária
litros_necessarios = distancia / 12

# Saída formatada com três casas decimais
print("{:.3f}".format(litros_necessarios))
