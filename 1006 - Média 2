# Entrada das notas
A = float(input())
B = float(input())
C = float(input())

# Calculando a média ponderada
media = (A * 2 + B * 3 + C * 5) / 10

# Saída formatada
print("MEDIA =", "{:.1f}".format(media))
